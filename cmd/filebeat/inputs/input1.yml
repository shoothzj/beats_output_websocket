- type: log
  # Change to true to enable this input configuration.
  enabled: true
  # Paths that should be crawled and fetched. Glob based paths.
  paths:
    - /Users/akka/go/src/beats_output_websocket/cmd/filebeat/example.log